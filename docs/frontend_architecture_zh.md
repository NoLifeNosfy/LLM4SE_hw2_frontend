# 🧱 前端架构设计文档

**项目名称：** Web-based AI Travel Planner  
**版本：** 1.0  
**作者：** [Your Name]  
**日期：** 2025-10-28  

---

## 1. 概述

本文档描述了 *Web-based AI 智能旅行规划系统* 前端部分的架构设计、技术栈选择与模块职责划分，旨在确保项目具备一致性、可扩展性和可维护性。

---

## 2. 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Vue 3 (Composition API) | 主框架，组件化开发 |
| 构建工具 | Vite | 极速构建与热更新 |
| 状态管理 | Pinia | 全局状态管理（取代 Vuex） |
| UI 组件库 | Element Plus | 提供基础组件与交互样式 |
| 地图组件 | vue-baidu-map-3x | 百度地图 Vue3 封装 |
| HTTP 客户端 | Axios | 统一请求封装 |
| 国际化 | vue-i18n | 多语言支持（中英） |
| 图表库 | ECharts | 预算与消费可视化 |
| 认证方式 | JWT | 与后端统一的身份验证 |
| 代码规范 | ESLint + Prettier | 统一代码风格 |
---

## 3. 项目结构

```
src/
 ├── api/ # REST API 封装
 │   ├── config.ts # Axios 配置与拦截器（JWT）
 │   ├── user.ts
 │   ├── trip.ts
 │   └── budget.ts
 ├── assets/
 ├── components/
 │   ├── Map/
 │   │   ├── MapContainer.vue
 │   │   ├── MapMarkers.vue
 │   │   └── MapRoute.vue
 │   ├── VoiceInput.vue
 │   └── BudgetChart.vue
 ├── pages/
 │   ├── Home.vue
 │   ├── Login.vue
 │   ├── Planner.vue
 │   ├── Budget.vue
 │   └── MapView.vue
 ├── store/
 │   ├── userStore.ts
 │   ├── tripStore.ts
 │   └── budgetStore.ts
 ├── router/
 │   └── index.ts
 ├── i18n/
 │   ├── en.json
 │   └── zh.json
 ├── utils/
 │   ├── speech.ts
 │   └── helpers.ts
 ├── App.vue
 └── main.ts
```

---

## 4. 模块职责

| 模块 | 职责 |
|------|------|
| **App.vue** | 根布局与路由出口 |
| **api/** | 封装所有 REST API 调用 |
| **store/** | 全局状态管理（用户、行程、预算） |
| **pages/** | 页面逻辑与 UI 展示 |
| **components/** | 可复用组件（地图、图表、语音输入） |
| **MapContainer.vue** | 初始化并管理百度地图 |
| **MapMarkers.vue** | 渲染景点标记 |
| **MapRoute.vue** | 展示行程路线 |
| **VoiceInput.vue** | 语音输入与 AI 请求 |
| **BudgetChart.vue** | 显示预算可视化图表 |
| **i18n/** | 国际化语言文件 |
| **router/** | 路由配置 |
| **utils/** | 工具函数封装 |

---

## 5. 数据流与交互

用户输入 → 前端组件 → 调用 API → 后端返回 JSON → Pinia 存储 → 界面响应更新

---

## 6. 开发规范

- 使用 **ESLint + Prettier** 进行代码格式化  
- 文件命名：小写中划线（如 `trip-planner.vue`）  
- 组件命名：PascalCase（如 `MapContainer.vue`）  
- Git 提交遵循 Conventional Commits 规范  

---

## 7. 安全与性能

| 分类 | 策略 |
|------|------|
| **认证** | 使用 JWT 鉴权 |
| **HTTPS** | 所有请求通过 HTTPS |
| **性能优化** | 懒加载组件、异步加载地图 |
| **缓存** | LocalStorage + Pinia 持久化 |
| **输入验证** | 防止恶意输入与注入 |

---

## 8. 后端集成与认证机制
前端通过 REST 接口与后端的 user、trip、event、location、route、expense 模块交互。
所有请求通过 Axios 拦截器自动携带 JWT Token 实现认证。
